此版本做到了：
（1）使用配置文件方式替代了手动修改客户端代码，即客户端操作符发生改变时，无需修改client端代码。
	此版本中我们是通过反射+配置文件方式。用户可以直接修改配置文件方式来修改操作符。
	或者可以考虑client端的图形化界面自主选择operate，然后存储到数据库中，
	然后后台从数据库中获取client传递的操作符。

配置文件选择了2中：
1）value值是全类名。通过它直接运行newInstance方法
2）value值是字符串类型的operate参数，获取参数后，还要通过switch来判断创建哪个对象。

问题：如何去掉switch？？？

