此版本做到了：
（1）计算和显示分离，即caculate和client。实现了面向对象的封装
（2）对于计算来说，各种计算时独立的。互不影响的。可以动态的添加或删除（工厂的switch中代码会受到影响）。


1、jdk1.8中switch使用字符串报错，所以此包中的代码考虑用jdk1.7

2、jdk1.8解决方案：将type从原来的字符串改成枚举类型就可以
       
       参考：http://blog.csdn.net/zhaojw_420/article/details/53735879
       
3、maven中有个插件可以判断是否用jdk的不同版本？？？

